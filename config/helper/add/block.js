export {blockAdd};

async function blockAdd(scriptDir) {
    // Todo block handling logic here
    // console.log('Block:', argsObj.values.block);
    console.log(colors.style('This feature currently in development!', colors.fg.yellow));
}